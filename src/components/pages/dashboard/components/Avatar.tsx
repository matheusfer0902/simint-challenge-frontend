/**
 * Re-export from the canonical shared location.
 * Dashboard components that import from "./Avatar" continue to work without changes.
 */
export { Avatar } from "@/components/shared/Avatar";
export type { AvatarProps } from "@/components/shared/Avatar";
